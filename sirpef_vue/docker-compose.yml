version: "3.9"
services:
  oac_vue_mppef:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:81"
    environment:
      NODE_ENV: production
    container_name: oac_vue
